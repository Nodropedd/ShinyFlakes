{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core window permissions only. No filesystem, shell, http, dialog, or clipboard plugin is exposed to the webview; every privileged operation goes through an explicit command in src/ipc.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
